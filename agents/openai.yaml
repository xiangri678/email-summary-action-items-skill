interface:
  display_name: "Email Intelligence Digest"
  short_description: "Summarize mail with thread context and evidence"
  default_prompt: "Review my authorized mailboxes for the requested period and produce a source-linked digest without changing mailbox state."
